// ======================================================================
// Planos — rotas canônicas (público vs autenticado)
// ======================================================================

/** Visualização/comparação pública — SignUp, landing, visitantes. */
export const PUBLIC_PLANS_PATH = "/planos";

/** Gestão de assinatura — área autenticada Perfil > Assinatura. */
export const AUTHENTICATED_PLANS_PATH = "/perfil/assinatura/planos";

/** Destino comercial compartilhado (coluna esquerda do SignUp). */
export const SIGNUP_PLANS_CANONICAL_PATH = PUBLIC_PLANS_PATH;
