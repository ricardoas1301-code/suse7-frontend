// ======================================================================
//  PERFIL — PREFERÊNCIAS (CONTAINER)
//  Objetivo: Centralizar as configurações do usuário (notificações, gerais)
// ======================================================================

import { Outlet } from "react-router-dom";

export default function Preferencias() {
  return (
    <div>
      <Outlet />
    </div>
  );
}
